# Locust configuration file
# Usage: locust --config=locust.conf

# Host (override with --host flag)
host = http://localhost:8000

# Web UI settings
web-host = 0.0.0.0
web-port = 8089

# Test settings (for headless mode)
users = 100
spawn-rate = 10
run-time = 5m

# Logging
loglevel = INFO
